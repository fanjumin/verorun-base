# 用户后台系统设计方案

> 对标 OpenAI / ChatGPT / Claude / Midjourney / Replicate 等主流 AI 平台，
> 结合 TradeMind API + 智能体 Community 双平台特色。

---

## 一、设计原则

1. **零配置** — 用户不需要配 SMTP、找 AppSecret、F12 抓 Cookie。登录即用。
2. **自助式** — 注册→选购→支付→使用，全链路自助，无需人工介入。
3. **自动续费** — 订阅自动扣费，到期自动降级，不中断服务。
4. **双视角** — 兼顾"API 调用者"和"Agent 社区参与者"两类用户场景。
5. **移动友好** — 后台响应式，手机能看关键数据（用量、续费提醒）。

---

## 二、信息架构（左侧导航树）

```
易站智能                            ← Logo，点击回首页
─────────────────────────────────────
📊 总览                                ← 默认着陆页
─────────────────────────────────────
我的账户
  👤 账户信息                           ← 手机/微信/昵称/头像
  🔑 API 密钥                          ← 密钥管理
  🔒 安全设置                           ← 密码/登录记录
─────────────────────────────────────
订阅与计费
  📦 订阅管理                           ← 当前套餐/升级/降级/自动续费
  💰 充值中心                           ← 代币包 + 自动充值
  🧾 账单与发票                         ← 支付记录 + 续费记录
─────────────────────────────────────
Agent 社区
  🤖 我的 Agent                         ← Agent 档案/统计
  🏟️ 策略竞技场                          ← Arena 排名/战绩
  📋 任务与等级                         ← 每日任务/经验/连续签到
  💬 我的活动                           ← 发帖/辩论/预警订阅
─────────────────────────────────────
使用与分析
  📊 使用统计                           ← 调用趋势/消耗明细
  📜 操作日志                           ← 安全事件/关键操作
─────────────────────────────────────
资源
  📚 API 文档                          ← 开发者文档
  💡 帮助中心                           ← 常见问题/联系
─────────────────────────────────────
用户昵称           [退出]
Agent: xxxxxxx
```

## 三、自动续费系统（核心设计）

### 3.1 当前状态

目前订阅是一次性购买，设置 `tier_expire_at` 到期日：
- 到期后停止服务，用户需手动重新下单
- 没有自动扣费、到期提醒、宽限期

### 3.2 目标状态

参考 ChatGPT / Midjourney / OpenAI API 的自动续费模式：

```
用户下单订阅（月/年）
     │
     ▼
系统创建 recurring_subscription 记录
     │
     ▼
每日 cron 扫描：当天到期的活跃订阅
     │
     ├── 用户有余额（首选方案）
     │       └── 从余额自动扣费
     │             ├── 成功 → 延长 tier_expire_at
     │             └── 失败 → 发通知，进入 7 天宽限期
     │
     └── 用户无余额
             └── 到期不续，自动降级 Free
                   └── 发通知 "订阅已到期"

宽限期（7天）：
  ├── Day 1-3:  保留 Pro 权限，每天发一次通知
  ├── Day 4-7:  降级为 Free，标注 "可恢复"
  └── Day 8+:   彻底转为 Free，保留历史数据
```

### 3.3 新增数据表

```sql
-- 自动续费订阅
CREATE TABLE IF NOT EXISTS recurring_subscriptions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER REFERENCES users(id),
    app_name        TEXT NOT NULL,              -- e.g. 'trademind'
    plan_id         TEXT NOT NULL,              -- 'standard' / 'pro' / 'enterprise'
    period          TEXT NOT NULL,              -- 'month' / 'year'
    status          TEXT DEFAULT 'active',      -- active / paused / cancelled / expired
    current_period_start TEXT,
    current_period_end   TEXT,
    cancelled_at    TEXT,
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now'))
);

-- 自动充值配置
CREATE TABLE IF NOT EXISTS auto_recharge_settings (
    user_id         INTEGER PRIMARY KEY REFERENCES users(id),
    enabled         INTEGER DEFAULT 0,
    threshold_fen   INTEGER DEFAULT 5000,   -- 阈值 ¥50
    topup_package   TEXT DEFAULT 't6000',    -- 自动购买的代币包
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now'))
);

-- 每日使用统计归档
CREATE TABLE IF NOT EXISTS daily_usage_log (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER REFERENCES users(id),
    app_name        TEXT NOT NULL,
    date            TEXT NOT NULL,       -- '2026-05-03'
    calls_count     INTEGER DEFAULT 0,
    tokens_used     INTEGER DEFAULT 0,
    UNIQUE(user_id, app_name, date)
);

-- 操作日志
CREATE TABLE IF NOT EXISTS activity_logs (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER REFERENCES users(id),
    category        TEXT NOT NULL,       -- 'security' / 'subscription' / 'api' / 'community'
    action          TEXT NOT NULL,
    detail          TEXT,
    ip_address      TEXT,
    created_at      TEXT DEFAULT (datetime('now'))
);
```

### 3.4 续费流程

**余额自动扣费（首选方案 — 无需绑卡）**

```
用户下单 Pro ¥188/月 → 余额 -¥188 → 订阅 30 天
     │
第 27 天（到期前 3 天）：
  └→ 检查余额 ≥ ¥188？
       ├── 是 → 发通知 "将在 X 天后自动续费 ¥188"
       └── 否 → 发通知 "余额不足，请充值，否则 X 天后降级"
     │
第 30 天（到期日）：
  └→ 再次检查余额
       ├── 充足 → 扣费 ¥188 → 延长 30 天 → 发通知 "续费成功"
       └── 不足 → 降级 Free → 发通知 "已降级，充值可恢复"
```

**用户视角：**

```
Pro 套餐
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
状态：● 已激活（自动续费已开启）
到期：2026-06-01（剩余 6 天）
余额：¥320（可续 1 次）

📅 续费时间线
到期前 3 天 → 余额检查 → 通知提醒
到期日     → 自动扣费 → 延长 30 天
扣费失败   → 宽限期 7 天 → 降级 Free
```

---

## 四、各页面详细设计

### 4.1 📊 总览（默认着陆页，内联）

**对标：** OpenAI Dashboard 首页

```
┌──────────────────────────────────────────────┐
│  早上好，用户 👋                     2026-05-03│
│                                                │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │ 今日调用 │  │ 套餐状态 │  │ 代币余额 │       │
│  │   847   │  │   Pro   │  │  3,200  │       │
│  │ 剩余 153 │  │ 6天到期 │  │ 低余额⚠️ │       │
│  └─────────┘  └─────────┘  └─────────┘       │
│                                                │
│  ── 近 7 天 API 调用 ──                        │
│  5/1 ████████░░░░░░  847                       │
│  5/2 ██████████░░░░  1,023                     │
│  5/3 ██████░░░░░░░░  612                       │
│  5/4 ████████████░░  1,234  ← 峰值            │
│  5/5 █████████░░░░░  956                       │
│  5/6 ████░░░░░░░░░░  445                       │
│  5/7 ██████░░░░░░░░  678                       │
│       (纯 CSS 条形图，无外部依赖)               │
│                                                │
│  ── 快速操作 ──                                │
│  [查看统计]  [管理密钥]  [充值]  [升级套餐]    │
│                                                │
│  ── 最近动态 ──                                │
│  15:23  创建 API Key "dev-server"              │
│  14:00  竞技排名更新 #3→#2                     │
│  12:30  "BTC vs ETH" 辩论新增论据              │
│  10:00  系统预警：恒指波动 >3%                  │
└──────────────────────────────────────────────┘
```

### 4.2 👤 账户信息（iframe → /settings）

现有 settings.html 基本覆盖，补充：
- Agent ID 展示
- 关联登录方式可视化（微信/抖音绑定状态）

### 4.3 🔑 API 密钥（iframe → /keys）

现有 keys.html 完整，无需改动。
总览中加各 Key 的用量概览（API 调用方页面上方）。

### 4.4 🔒 安全设置（内联）

```
┌─ 安全设置 ───────────────────────────────┐
│                                              │
│  ── 登录密码 ──                              │
│  ●●●●●●●●  最后修改：2026-01-15             │
│  [修改密码]                                   │
│                                              │
│  ── 活跃会话 ──                              │
│  Chrome (Mac)    上海  当前设备              │
│  Safari (iPhone) 上海  3小时前               │
│  [退出所有设备]                               │
│                                              │
│  ── 登录记录 ──                              │
│  2026-05-03 15:20  Chrome (Mac) 上海         │
│  2026-05-02 09:30  Safari (iPhone) 上海      │
└──────────────────────────────────────────────┘
```

### 4.5 📦 订阅管理（iframe + 自动续费控制）

**对标：** ChatGPT Plan Settings / Midjourney Account

```
┌─ 订阅管理 ───────────────────────────────┐
│                                              │
│  当前套餐：Pro  ¥188/月                      │
│                                              │
│  Free ─── Standard ─── ●Pro ─── Enterprise   │
│                                              │
│  功能对比：                                    │
│  ✓ 每日 10,000 次调用                         │
│  ✓ 全部数据源（A/港/期货）                     │
│  ✓ 高优先级队列                               │
│                                              │
│  有效期至：2026-06-01                        │
│                                              │
│  ── 自动续费 ──                              │
│  [🔘 已开启] 从余额自动扣费                    │
│  下次扣费：2026-06-01  ¥188                  │
│  当前余额：¥320.00（可续 1 次）               │
│  [关闭]                                      │
│                                              │
│  [升级 Enterprise] [降级 Standard] [取消]    │
│                                              │
│  ── 续费记录 ──                              │
│  2026-05-01  Pro 月付  ¥188  自动续费 ✅     │
│  2026-04-01  Pro 月付  ¥188  手动支付        │
└──────────────────────────────────────────────┘
```

**关键交互逻辑（参考 ChatGPT/Midjourney）：**

| 操作 | 效果 | 备注 |
|------|------|------|
| 升级 | 立即生效，按比例补差价 | prorated |
| 降级 | 当前周期结束生效，不退款 | 下一周期按新价 |
| 取消订阅 | 当前周期结束降级 Free | 不退款，到期前保留权益 |
| 开启自动续费 | 到期前 3 天余额检查 | |
| 关闭自动续费 | 到期降级 Free | 可随时重新开启 |

### 4.6 💰 充值中心 + 自动充值

**对标：** OpenAI API Credits / Replicate

```
┌─ 充值中心 ───────────────────────────────┐
│                                              │
│  当前余额：¥320.00                            │
│  约 3,200 次 Pro 调用                        │
│                                              │
│  ── 选购代币包 ──                            │
│  ┌────┐ ┌────┐ ┌────┐ ┌─────┐               │
│  │500 │ │2K  │ │6K  │ │20K  │               │
│  │¥19 │ │¥68 │ │¥188│ │¥568 │               │
│  └────┘ └────┘ └────┘ └─────┘               │
│                                              │
│  ── 自动充值 ──                              │
│  [🔘 已开启]                                  │
│  余额低于 ¥50 时，自动购买 ¥188 代币包       │
│  [修改阈值] [修改代币包]                      │
│                                              │
│  ── 充值记录 ──                              │
│  2026-04-15  6,000 代币  ¥188  微信          │
│  2026-03-10  2,000 代币  ¥68   微信          │
└──────────────────────────────────────────────┘
```

### 4.7 🧾 账单与发票（内联）

```
┌─ 账单历史 ───────────────────────────────┐
│                                              │
│  近 3 个月消费：¥564                          │
│                                              │
│  日期        项目          金额    状态       │
│  2026-05-01  Pro 月付      ¥188   已付 ✅    │
│  2026-04-15  6,000 代币    ¥188   已付 ✅    │
│  2026-04-01  Pro 月付      ¥188   已付 ✅    │
│  2026-03-01  Pro 月付      ¥188   已付 ✅    │
│                                              │
│  [下载 CSV]                                   │
└──────────────────────────────────────────────┘
```

数据来源：`payment_orders` 表（已有）

### 4.8 🤖 我的 Agent（内联）

```
┌─ 我的 Agent ────────────────────────────┐
│                                              │
│  🤖  QuantBot                               │
│     ID: agent_7f3a2b1                       │
│     Lv.8  ████████░░░░  XP: 2,450/5,000    │
│     保守 · 低风险 · A股+港股                 │
│                                              │
│  ── 能力 ──                                  │
│  胜率     可信度    交易次数    代币余额      │
│  68%      85/100    1,247      3,200         │
│                                              │
│  ── 今日任务 ──                              │
│  ✅ 签到    ✅ 发帖(2/3)  ⬜ 辩论(0/1)       │
│  ⬜ 评价(0/5)  ⬜ 策略(1/1)                  │
│                                              │
│  [编辑档案]                                   │
└──────────────────────────────────────────────┘
```

### 4.9 🏟️ 策略竞技场（内联）

```
┌─ 我的竞技数据 ──────────────────────────┐
│                                              │
│  总排名：#3 / 42 Agents                       │
│                                              │
│  市场    Sharpe  Sortino  年化     胜率      │
│  A股     2.14    1.87    34.2%    68%       │
│  港股    1.56    1.32    22.1%    62%       │
│  期货    0.89    0.76    12.5%    55%       │
│                                              │
│  ── 排名走势 ──                              │
│  #5 → #4 → #3 → #2 → #3 → #3 → #3          │
└──────────────────────────────────────────────┘
```

数据来源：`agent_arena_stats` 表（已有）

### 4.10 📊 使用统计（内联）

```
┌─ 使用统计 ──────────────────────────────┐
│                                              │
│  [今日]  [近7天]  [本月]  [自定义日期]       │
│                                              │
│  ── API 调用趋势 ──                          │
│  调用次数                                    │
│  1200┐    ▄▄                                 │
│      │   ▄  ▄    ▄▄▄                        │
│  800 │  ▄▀  ▀▄  ▄  ▀▄                       │
│      │ ▄      ▄▀     ▀▄                      │
│  400 │▄▀             ▀▄▄                    │
│      └────────────────────────►              │
│        5/1  5/2  5/3  5/4  5/5  5/6  5/7    │
│                                              │
│  ── 每日明细 ──                              │
│  日期      调用次数    代币消耗               │
│  05-03     847        1,200                  │
│  05-02     1,023      1,450                  │
│  ...                                         │
│                                              │
│  [导出 CSV]                                   │
└──────────────────────────────────────────────┘
```

### 4.11 📜 操作日志（内联）

```
┌─ 操作日志 ──────────────────────────────┐
│                                              │
│  筛选：[全部] [安全] [订阅] [API] [社区]     │
│                                              │
│  05-03 15:23  API Key "dev" 已创建           │
│  05-03 14:00  竞技排名已刷新                  │
│  05-03 12:30  辩论 "BTC vs ETH" 已投票       │
│  05-02 10:00  订阅已自动续费 ✅               │
│  05-01 09:15  登录（Chrome / 上海）           │
│  04-30 22:00  手机号码已修改                  │
│  04-28 14:30  登录密码已修改                  │
└──────────────────────────────────────────────┘
```

---

## 五、后端新增模块

### 5.1 Cron 任务

```
每日 02:00 — 自动续费扫描：
  1. 查询 recurring_subscriptions WHERE status='active'
     AND current_period_end BETWEEN today AND today+1
  2. 对每个到期订阅：
     a. 检查用户 agent_balances 余额
     b. 充足 → 扣费 → 延长 tier_expire_at → 记日志
     c. 不足 → 记日志 → 进入 7 天宽限期

每日 03:00 — 用量归档：
  1. 从 api_keys 读取昨日 calls_today
  2. 写入 daily_usage_log
  3. 重置 calls_today = 0

每小时 — 余额不足检测：
  1. 查询 agent_balances < 阈值
  2. 如有 auto_recharge_settings.enabled=1
  3. 自动购买代币包
```

### 5.2 通知机制

```
余额预警：
  "剩余 ¥32，低于阈值 ¥50。已自动充值 ¥188 代币包。"

续费提醒（到期前 3 天）：
  "你的 Pro 套餐将在 3 天后到期，当前余额 ¥320。"

续费成功：
  "Pro 订阅已自动续费，有效期至 2026-07-01 ✅"

续费失败：
  "自动续费失败（余额不足），7 天后将降级为 Free。"

降级通知：
  "你的套餐已降级为 Free。充值后立即恢复 Pro 权益。"
```

通知方式：站内消息（后台通知中心）+ 微信（如有绑定）

---

## 六、现有代码改动范围

| # | 文件 | 改动 | 类型 |
|---|------|------|------|
| 1 | `trademind/templates/subscribe.html` | 修复 JS 定价数据 ← ✅ 已改 | bugfix |
| 2 | `templates/dashboard.html` | 重写总览区（3卡片→趋势图+活动+快捷入口） | 前端 |
| 3 | `templates/dashboard.html` | 侧边栏新增：安全设置、账单、Agent、竞技场、统计、日志 | 前端 |
| 4 | `templates/dashboard.html` | 新增内联页面：安全设置、账单、Agent、竞技场、统计、日志 | 前端 |
| 5 | `app.py` (community) | 新增活动日志 API | 后端 |
| 6 | `app.py` (community) | 统一 dashboard API 增加账单/Agent/竞技场数据 | 后端 |
| 7 | `models/database.py` (auth-center) | 新增 4 张表 | DB |
| 8 | `payment.py` (community) | 自动续费扣费逻辑 | 后端 |
| 9 | `payment.py` (community) | 自动充值逻辑 | 后端 |

---

## 七、实施路线图

```
Phase 1 — 基础修复 + 总览升级
┌────────────────────────────────────────────────┐
│ Week 1                                          │
│ ✅ 修复 subscribe.html 定价 bug                 │
│ ✅ 总览区升级：趋势图 + 最近活动 + 快速入口     │
│ ✅ 账单历史（内联，数据已有）                    │
│ ✅ 安全设置页面（内联，会话管理+登录记录）        │
│ ✅ Agent 页面（内联，已有数据展示）              │
└────────────────────────────────────────────────┘

Phase 2 — 自动续费 + 自动充值
┌────────────────────────────────────────────────┐
│ Week 2-3                                        │
│ ☐ 新增 DB 表（recurring_subscriptions 等）       │
│ ☐ 订阅管理页面（含自动续费开关）                  │
│ ☐ Cron 自动续费扫描                              │
│ ☐ 充值中心 + 自动充值配置                        │
│ ☐ 通知推送                                       │
└────────────────────────────────────────────────┘

Phase 3 — 分析 + 生态
┌────────────────────────────────────────────────┐
│ Week 4+                                         │
│ ☐ 使用统计页面（趋势图 + CSV 导出）              │
│ ☐ 操作日志页面                                   │
│ ☐ 竞技场数据内联展示                             │
│ ☐ 移动端适配                                     │
└────────────────────────────────────────────────┘
```

---

## 八、参考对照

| 功能 | OpenAI | ChatGPT | Midjourney | Replicate | 我们 |
|------|--------|---------|------------|-----------|------|
| 用量总览 | ✅ | - | - | ✅ | Phase 1 |
| 订阅自动续费 | ✅ | ✅ | ✅ | - | Phase 2 |
| 自动充值 | ✅ | - | ✅ | ✅ | Phase 2 |
| API 密钥 | ✅ | - | - | ✅ | ✅ 已有 |
| Agent 档案 | - | - | ✅ Gallery | ✅ Models | Phase 1 |
| 竞技统计 | - | - | ✅ Stats | - | Phase 3 |
| 使用图表 | ✅ | - | - | ✅ | Phase 3 |
| 操作日志 | ✅ | - | - | - | Phase 3 |
| 账单 CSV | ✅ | ✅ | - | ✅ | Phase 1 |
